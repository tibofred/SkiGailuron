imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: fr

framework:
    #esi: ~
    translator: { fallbacks: ['%locale%'] }
    secret: '%secret%'
    router:
        resource: '%kernel.root_dir%/config/routing.yml'
        strict_requirements: ~
    form: ~
    csrf_protection: ~
    validation: { enable_annotations: true }
    #serializer: { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale: '%locale%'
    trusted_hosts: ~
    #trusted_proxies: ~
    session:
        # http://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id:  session.handler.native_file
        save_path:   "%kernel.root_dir%/../var/sessions/%kernel.environment%"
    fragments: ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

# Twig Configuration
twig:
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'bootstrap_3_layout.html.twig'
    globals:
        webmaster: <a href="https://viweb.ca" title="Conception site web - ViWeb" target="_blank">ViWeb</a>
        compagnie: Gai Luron
        description: Centre de ski de fond 
        type_compagnie: Centre de ski de fond 
        adresse_civique: 2164 montée Sainte-Thérèse
        ville: Saint-Jérôme
        code_postal: J5L 0L5
        province: Québec
        telephone: 450 224-5302
        fax: 450 224-9994
        courriel: info@skigailuron.ca
        url: https://skigailuron.ca
        lundi: 9h à 17h
        mardi: 9h à 17h
        mercredi: 9h à 17h
        jeudi: 9h à 17h
        vendredi: 9h à 17h
        samedi: 8h à 17h
        dimanche: 8h à 17h
        #https://itouchmap.com/latlong.html pour avoir les coordonnées lat/long
        latitude: 45.844108
        longitude: -74.075317,8
        facebookid: 285397091817919
        facebooklink: https://www.facebook.com/skidefondgailuron
        googlepluslink: https://plus.google.com/101706580077432308012
        linkendinlink: 

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_mysql
        host: '%database_host%'
        port: '%database_port%'
        dbname: '%database_name%'
        user: '%database_user%'
        password: '%database_password%'
        charset: UTF8
        unix_socket: "/Applications/MAMP/tmp/mysql/mysql.sock"
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/../var/data/data.sqlite"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #path: '%database_path%'

    orm:
        auto_generate_proxy_classes: '%kernel.debug%'
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: '%mailer_transport%'
    host: '%mailer_host%'
    username: '%mailer_user%'
    password: '%mailer_password%'
    spool: { type: memory }

fos_user:
    db_driver:     orm                       # Le type de BDD à utiliser, nous utilisons l'ORM Doctrine depuis le début
    firewall_name: main                      # Le nom du firewall derrière lequel on utilisera ces utilisateurs
    user_class:    VW\UserBundle\Entity\User # La classe de l'entité User que nous utilisons
    from_email:
        address: creationsiteweb27@gmail.com
        sender_name: Site web - Gai-Luron

welp_mailchimp:
    api_key: c374e1368b84fa7f99d5cfa5e8d69c0f-us5

payum:
    security:
        token_storage:
            PaymentBundle\Entity\PaymentToken: { doctrine: orm }
    storages:
        PaymentBundle\Entity\Payment: { doctrine: orm }
    gateways:
        paypal_express_checkout_and_doctrine_orm:
            factory: paypal_express_checkout
            username: "%paypal_creditentials_username%"
            password: "%paypal_creditentials_password%"
            signature: "%paypal_creditentials_signature%"
            sandbox: false
jms_i18n_routing:
    default_locale: fr
    locales: [fr, en]
    #strategy: prefix
    strategy: prefix_except_default

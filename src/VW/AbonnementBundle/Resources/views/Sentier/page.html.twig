{% extends "VWBaseBundle::layout.html.twig" %}

{% block body %}

    <!-- Caroussel
        ||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||
    -->
    <div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval="7500">
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
        </ol>

        <div class="carousel-inner" role="listbox">
            <div class="carousel-item active">
                <img class="first-slide" src="{{ absolute_url(asset('/images/carousel/hebergement/serveur-web.jpg')) }}"
                     alt="E-Commerce - Conception site web">
                <div class="container">
                    <div class="carousel-caption d-none d-md-block text-left">
                        <h4>Location et boutique de ski</h4>
                        <p>Des serveurs près de votre clientèle améliore le référencement local.</p>
                        <p><a class="btn btn-lg btn-primary" href="{{ path('vw_contact_homepage') }}" role="button">Nous
                                contacter</a></p>
                    </div>
                </div>
            </div>

            <div class="carousel-item">
                <img class="second-slide"
                     src="{{ absolute_url(asset('/images/carousel/hebergement/gestion-courriel.jpg')) }}"
                     alt="Second slide">
                <div class="container">
                    <div class="carousel-caption d-none d-md-block text-left">
                        <h4>Gestion de courriel</h4>
                        <p>Laissez un techinicien d'expérience configurer vos courriels.</p>
                        <p><a class="btn btn-lg btn-primary" href="{{ path('vw_contact_homepage') }}" role="button">Contact</a>
                        </p>
                    </div>
                </div>
            </div>

            <div class="carousel-item">
                <img class="third-slide" src="{{ absolute_url(asset('/images/carousel/hebergement/sauvegarde.jpg')) }}"
                     alt="Third slide">
                <div class="container">
                    <div class="carousel-caption d-none d-md-block text-right">
                        <h4 class="red">Sauvegarde journalière</h4>
                        <p style="color:black;">Ayez la conscience tranquille.</p>
                        <p><a class="btn btn-lg btn-primary" href="{{ path('vw_contact_homepage') }}" role="button">Contact</a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
        <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>


    <!-- Contenu de la page
        ||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||
    -->
    <div class="container pages">
        <div class="row">
            <div class="col-sm-12">
                <h1>Sentiers du centre de ski de fond</h1>
                <p>Pour les adeptes de ski de fond, 27.2 km de sentiers sont entretenus, et pour les randonneurs, 12 km
                    de sentiers de raquette sont également disponibles.</p>

                <p>Appréciez le bonheur d’une journée nature et découvrez les joies du plein air dans un environnement
                    sécuritaire et aménagé. Pour tous les niveaux avec des dénivelés adaptés à vos envies.</p>

                <p>Nous avons aménagé un sentier uniquement pour le Fatbike de 7 kM</p>

                <p>* Les Fatbike ne sont pas autorisés à circuler dans les sentiers de raquette et de ski de fond.</p>
            </div>

        </div>
    </div>

    <div class="container">
        <div class="row">

            <div class="col-sm-8">
                <div id="sentiers">
                    <span class="icon"></span>
                    <img id="imgSentier" src="{{ absolute_url(asset('/images/sentiers/Tous-les-sentiers.jpg')) }}" alt="">
                </div>
            </div><!-- end col -->


            <div class="col-sm-4" id="liste-sentiers">

                <h3>PISTE CLASSIQUE</h3>
                <ul>
                    {% for sentier in sentiers if sentier.sport.id == ski_de_fond_classique.id %}
                        <li><a href="{{ path('vw_sentier_piste_ski_de_fond', { 'slug': sentier.slug }) }}" rel="sentiers-ski-de-fond-{{ sentier.slug }}">{{ sentier.nom }} - {{ sentier.longueur }} km</a></li>
                    {% endfor %}
                </ul>
                <hr>

                <h3>PISTE PAS DE PATIN</h3>
                <ul>
                    {% for sentier in sentiers if sentier.sport.id == ski_de_fond_sans_patin.id %}
                        <li><a href="{{ path('vw_sentier_piste_ski_de_fond', { 'slug': sentier.slug }) }}" rel="sentiers-ski-de-fond-{{ sentier.slug }}">{{ sentier.nom }} - {{ sentier.longueur }} km</a></li>
                    {% endfor %}
                </ul>

                <hr>

                <h3>RAQUETTE</h3>
                <ul>
                    {% for sentier in sentiers if sentier.sport.id == raquette.id %}
                        <li><a href="{{ path('vw_sentier_piste_raquette', { 'slug': sentier.slug }) }}" rel="sentiers-raquette-{{ sentier.slug }}">{{ sentier.nom }} - {{ sentier.longueur }} km</a></li>
                    {% endfor %}
                </ul>

            </div><!-- end col -->
        </div><!-- end row -->

    </div><!-- end container -->

    <script src="//ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

    <script>
        var current_img = "";
        $.noConflict();
        jQuery(document).ready(function ($$) {

            $("#liste-sentiers").find("ul > li > a").on('mouseover', function () {

                $$("#imgSentier").stop();
                $$("#imgSentier").clearQueue();

                var img_name = $(this).attr('rel');

                if (img_name === "") {
                    img_name = "Tous-les-sentiers";
                }

                if (current_img !== $(this).attr('rel')) {
                    $$("#imgSentier").fadeOut(200, function () {
                        current_img = "/images/sentiers/" + img_name + ".jpg";
                        $$("#imgSentier").attr('src', "../images/sentiers/" + img_name + ".png");
                    }).fadeIn(200);
                }
            });

            $("#liste-sentiers").find("ul > li > a").on('mouseout', function () {
                $$("#imgSentier").stop();
                $$("#imgSentier").clearQueue();
                $$("#imgSentier").fadeOut(200, function () {
                    $$("#imgSentier").attr('src', "/images/sentiers/Tous-les-sentiers.jpg");
                }).fadeIn(200);
            });
        });

    </script>

{% endblock %}






{% extends "VWAbonnementBundle::layout.html.twig" %}

{% block title %}

 Conditions - {{ parent() }}

{% endblock %}

{% block vwclient_body %}
 
            
<h1>Liste des sports</h1>
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Nom (Français)</th>
                  <th>Nom (Anglais)</th>
                  <th>Modifier</th>
                  <th>Supprimer</th>
                </tr>
              </thead>
              
                
            <tbody>
                
                {% for condition in conditions %}
                <tr>
                  <td>{{ condition.nom }}</td>
                  <td>{{ condition.nomEn }}</td>
                  <td><a href="{{ path('vw_condition_modifier', {'id': condition.id}) }}" type="button" class="btn btn-success">Modifier</a></td>
                  <td><a href="{{ path('vw_condition_supprimer', {'id': condition.id}) }}" type="button" class="btn btn-danger">Supprimer</a></td>
                </tr>
                {% endfor %}
                
              </tbody>
            </table>
          </div>


{% endblock %}

